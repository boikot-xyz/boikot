{
  "name": "site",
  "version": "0.1.0",
  "description": "boikot.xyz",
  "main": "app.jsx",
  "scripts": {
    "start": "esbuild app.jsx --bundle --loader:.md=text --loader:.svg=file --servedir=www/ --outdir=www/build --public-path=/build --serve-fallback=www/index.html",
    "build": "esbuild app.jsx --bundle --loader:.md=text --loader:.svg=file --public-path=/build --minify --outdir=www/build"
  },
  "author": "boikot",
  "license": "gpl v3",
  "dependencies": {
    "esbuild": "^0.19.9",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router": "^6.21.1",
    "react-router-dom": "^6.21.1",
    "slugify": "^1.6.6",
    "styled-components": "^6.1.1"
  }
}
