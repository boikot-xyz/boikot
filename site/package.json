{
  "name": "site",
  "version": "0.1.0",
  "description": "boikot.xyz",
  "main": "app.jsx",
  "scripts": {
    "start": "esbuild app.jsx --bundle --servedir=www/ --outdir=www/js",
	"build": "esbuild app.jsx --bundle --minify --outdir=www/js"
  },
  "author": "boikot",
  "license": "gpl v3",
  "dependencies": {
    "esbuild": "^0.19.9",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
